<h2>Delete category</h2>
<div class="card">
    <div class="card-body">
        <h5>Are you sure you want to delete this category?</h5>
        <strong>Categoria: </strong>{{category.category}} <br>
        <strong>Slug: </strong><u>{{category.slug}}</u> <br>
        <small style="color: gray;">Created at {{#dateFormatter category.date 4}}{{formattedDate}}{{/dateFormatter}} - {{#dateFormatter category.date 5}}{{formattedDate}}{{/dateFormatter}}</strong>
        <form action="/admin/categories/delete" method="POST">
            <input name="category" type="hidden" value="{{category.category}}">
            <input name="slug" type="hidden" value="{{category.slug}}">
            <button type="submit" class="btn btn-outline-danger mt-2">Delete</button>
            
        </form>
    </div>
</div>
<a href="/admin/categories"><button class="btn btn-secondary mt-4">Back</button></a>